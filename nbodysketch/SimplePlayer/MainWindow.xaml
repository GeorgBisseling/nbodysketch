<Window x:Class="SimplePlayer.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="MainWindow" Height="350" Width="525">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Canvas Name="Universe"  Grid.Row="0"  VerticalAlignment="Stretch" HorizontalAlignment="Stretch" ClipToBounds="True" />
        <Label Name="Label" Grid.Row="1">TBD</Label>
        <CheckBox Name="PlayButton" Grid.Row="2" Click="PlayButton_Click" IsChecked="False">Play</CheckBox>
    </Grid>
</Window>
